<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    android:padding="@dimen/fragment_menu_10dp">

    <ImageView
        android:id="@+id/img_close"
        android:layout_width="@dimen/fragment_menu_30sp"
        android:layout_height="@dimen/fragment_menu_30sp"
        android:src="@drawable/baseline_cancel_24"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/txt_history"
        android:textColor="@color/black"
        android:textSize="@dimen/fragment_menu_18sp"
        app:layout_constraintTop_toBottomOf="@+id/img_close" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/list_math"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginVertical="@dimen/fragment_menu_5dp"
        app:layout_constraintTop_toBottomOf="@+id/tv_title" />
</androidx.constraintlayout.widget.ConstraintLayout>